Generated calendar import breakdowns are written to `backend/imports/generated/`.

Each imported calendar file or URL can produce a JSON artifact here after you confirm the preview import.

These generated JSON files are ignored by Git.
